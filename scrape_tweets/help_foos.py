def collect_users():
    """
    iterate over existing users in network
    check their loacition
    if cro location 
    check if already in base
    if no, add to list

    
    scrape that user tweets back untill 1.1.2020.
    """
    pass


def scrape_tweets_by_user_id(user_id):
    """
    Takes user ID and if profile is not protected, scrapes that user tweets back untill 1.1.2020.

    Returns dataframe of tweets
    """

    pass


def update_dataset():
    """
    Takes as input dataframe, if non empty dataframe,
    takes ids of tweets that are not in existing dataset and appends them in.
    """
    pass

